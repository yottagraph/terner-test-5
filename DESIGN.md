# terner test 5

## Vision

https://banks.data.fdic.gov/api/institutions — public JSON API, no API key, a few thousand records (small/fast fetch), and every record is an organization with a strong ID (FDIC cert number), a location, and clean fields.

please onboard this data source. 

## Status

Project just created. Run `/build_my_app` in Cursor to start building.

## Modules

*None yet — the agent will populate this as features are built.*
